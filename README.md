# 2D Convolution on Hardware

This repo contains simple code for convolution of type `valid` .

### Usage:

Just clone and open with Vivado software `.xpr` file.

In order to generate `conv_kernal.txt` and `conv_input.txt` follow the repo. [CLICK HERE](https://github.com/akifejaz/HW-Verification)



### TODOs:

- Shifting algo in testbench that moves the kernal to whole matrix
